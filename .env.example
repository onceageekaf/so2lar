# Production (Vercel → Settings → Environment Variables).
# Prefer full URL; hostname-only is OK (https:// is added automatically).
# NEXT_PUBLIC_SITE_URL=https://so2lar.ttos.ai
# NEXT_PUBLIC_SITE_URL=so2lar.ttos.ai
